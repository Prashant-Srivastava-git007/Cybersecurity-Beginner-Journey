
# 🔎 KAPE — Kroll Artifact Parser and Extractor (Quick Guide)

KAPE (Kroll Artifact Parser and Extractor) is a fast, triage-focused forensic tool for collecting and processing Windows artifacts — useful for incident response and initial investigations. This short guide shows how to install KAPE and perform a basic triage collection (KAPE Triage target).

---

## ⬇️ Download & Install
1. Create an account on Kroll and download the KAPE zip from:  
   `https://www.kroll.com/en/services/cyber/incident-response-recovery/kroll-artifact-parser-and-extractor-kape`
2. Extract the downloaded ZIP to a folder (e.g., `C:\Tools\KAPE`).

---

## ▶️ Take a Forensic Triage Capture (GUI method)
1. Open the KAPE folder, **right-click `gkape.exe` → Run as administrator**.  
2. In the GUI:
   - **Target Drive:** select `C:` (or the drive you want to triage).  
   - **Destination:** create and choose a folder on your Desktop (e.g., `C:\Users\<You>\Desktop\KAPE_Output`).  
   - Under **Targets**, scroll to **KAPE Triage** and check the box.  
   - Click **Execute**.  
3. Wait — the run can take several minutes. When finished, you’ll see a `Press any key to exit` message in the window. Press a key and close the GUI.  
4. The collected files and reports will be in your chosen destination folder.

---

## 🗂️ What you get
- A structured output folder containing collected artifacts (event logs, registry hives, prefetch, timelines, etc.) and any generated reports — ready for analysis with other forensic tools.

> **Short & safe:** KAPE is a powerful triage tool — run it only with authorization, collect outputs, verify them, and analyze in an isolated environment.

---
