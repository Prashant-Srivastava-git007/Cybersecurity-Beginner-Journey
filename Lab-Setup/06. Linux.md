# 🐧 Installing Linux in VirtualBox — Complete Beginner Guide

## ⚙️ What You Need
Before you start:
- **VirtualBox** – Already installed on your system.
- **Linux ISO File** – Download from:
  - [Kali Linux](https://www.kali.org/get-kali/#kali-virtual-machines)
    
---

## 🪟 Step 1: Create a New Virtual Machine

1. **Open VirtualBox** → click on **New**.
2. Enter:
   - **Name:** e.g., `Ubuntu-VM`
   - **Machine Folder:** Choose where to save the VM.
   - **Type:** Linux  
   - **Version:** Ubuntu (64-bit) *(select according to the ISO you downloaded)*  
3. Click **Next**.

---

## 🧠 Step 2: Allocate System Resources

1. **Memory (RAM):**  
   - Minimum → 2 GB (2048 MB)  
   - Recommended → 4 GB or more for smooth performance.
2. Click **Next**.

3. **Processor:**  
   - Minimum → 1 CPU  
   - Recommended → 2 or more (if available)  
   *(Don’t assign more than half your system’s total cores)*

---

## 💾 Step 3: Create a Virtual Hard Disk

1. Choose **Create a virtual hard disk now** → click **Create**.
2. Select **VDI (VirtualBox Disk Image)** → click **Next**.
3. Choose **Dynamically allocated** → this allows the disk to grow as needed.
4. Set **Disk Size:**  
   - Minimum → 25 GB  
   - Recommended → 40 GB+
5. Click **Create**.

---

## 📁 Step 4: Mount the Linux ISO

1. Select your new VM → click **Settings** → **Storage**.  
2. Under *Controller: IDE*, click on the **Empty disk icon**.  
3. On the right side, click the **CD icon** → **Choose a disk file…**  
4. Select the downloaded Linux `.iso` file.  
5. Click **OK**.

---

## 🚀 Step 5: Start the Virtual Machine

1. Select your VM → click **Start**.
2. The Linux ISO will boot. Choose **“Try or Install Ubuntu”** (or the equivalent for your chosen distro).
3. Once the installer opens, choose **Install Linux**.

---

## 🧭 Step 6: Follow the Installation Wizard

During the installation, follow these basic steps:

1. **Language:** Choose your preferred language → click **Continue**.
2. **Keyboard Layout:** Keep default or select your layout.
3. **Updates & Other Software:**
   - Choose **Normal Installation**.
   - Check **Install third-party software** (for drivers/codecs).
4. **Installation Type:**  
   - Select **Erase disk and install Linux** *(only affects the virtual hard disk, not your actual system!)*  
5. **Time Zone:** Select your city or region.
6. **User Setup:**
   - Enter a **username**, **computer name**, and **password**.
7. Click **Install Now** → confirm changes → installation begins.

---

## ⏳ Step 7: Wait for Installation to Finish

The setup will copy files and configure the system.  
When done, you’ll see a message like:

> **Installation Complete — Restart Now**

- Click **Restart Now**.
- When prompted to remove installation media, press **Enter**.

---

## 🧩 Step 8: First Boot & Post-Installation Setup

Once the VM restarts:
1. Log in using the username and password you created.  
2. You’ll be greeted by the Linux desktop! 🎉  
3. Run updates (important for security and stability):

```bash
sudo apt update && sudo apt upgrade -y
```

---

## Step 9: Install VirtualBox Guest Additions for smoother performance:
 * Go to Devices → Insert Guest Additions CD Image...
 * Open the mounted disk and run: `sudo ./VBoxLinuxAdditions.run`
 * This adds features like:
     * Shared clipboard (copy-paste between host & VM)
     * Full-screen resolution
     * Drag-and-drop file sharing

